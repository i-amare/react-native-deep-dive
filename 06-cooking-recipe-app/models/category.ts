class Category {
  id: string;
  title: string;
  colour: string;

  constructor(id: string, title: string, colour: string) {
    this.id = id;
    this.title = title;
    this.colour = colour;
  }
}

export default Category;
